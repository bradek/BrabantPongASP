@model IEnumerable<BrabantPongASP.Models.ToernooiScheidsrechter>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Toernooi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Scheidsrechter)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Toernooi.ToernooiNaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Scheidsrechter.ScheidsrechterNaam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ToernooiScheidsrechterId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ToernooiScheidsrechterId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ToernooiScheidsrechterId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
